# 评测结果

运行 `python scripts/run_eval.py` 后，详细输出会保存为 `reports/eval.json`。

提交公开仓库前，请在这里补充：

- 原始模型、Few-shot 和 LoRA 模型的对比表；
- 固定推理参数和软硬件环境；
- 至少 50 条人工盲评的方法与结果；
- 典型成功案例和失败案例；
- 数据泄漏检查方法。

不要提交未经实际运行的占位数字。

